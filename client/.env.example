# Base URL for the backend API.
# Dev: leave as /api — the Vite dev server proxies /api to the backend (see vite.config.ts).
# Production: set to your deployed API origin, e.g. https://api.sazia-crm.com/api
VITE_API_BASE_URL=/api
